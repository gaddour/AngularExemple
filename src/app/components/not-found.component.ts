import { Component, OnInit, NgZone } from '@angular/core';

@Component({
  selector: 'not-found',
  template: '<p> Content not found !!</p>'
})

export class NotFoundComponent {
    constructor() {
    }
}
